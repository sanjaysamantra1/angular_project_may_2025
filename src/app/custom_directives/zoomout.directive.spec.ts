import { ZoomoutDirective } from './zoomout.directive';

describe('ZoomoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomoutDirective();
    expect(directive).toBeTruthy();
  });
});
